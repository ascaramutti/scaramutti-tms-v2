# ==========================================
# Docker Compose - Producción v2
# Comparte la DB con v1 (scaramutti-tms-db-prod).
# Frontend expuesto en :8080 (v1 usa :80).
# ==========================================
version: '3.8'

services:
  # ==========================================
  # Backend (Quarkus)
  # ==========================================
  backend:
    build:
      context: ./backend
      target: production
    image: scaramutti-tms-v2-backend
    container_name: scaramutti-tms-v2-backend-prod
    restart: always
    env_file:
      - .env
    environment:
      QUARKUS_PROFILE: prod
    volumes:
      # Claves JWT montadas como volumen (no se copian al imagen)
      - ./keys:/app/keys:ro
    expose:
      - "8080"
    networks:
      - scaramutti-tms_default
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://127.0.0.1:8080/api/v1/q/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M

  # ==========================================
  # Frontend (Nginx + React)
  # ==========================================
  frontend:
    build:
      context: ./frontend
      target: production
      args:
        VITE_API_BASE_URL: /api/v1
    image: scaramutti-tms-v2-frontend
    container_name: scaramutti-tms-v2-frontend-prod
    restart: always
    ports:
      - "8080:80"
    networks:
      - scaramutti-tms_default
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://127.0.0.1/"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 256M

  # ==========================================
  # Gateway (un solo origin) — topología anterior a Azure, documentada como
  # referencia del rollback.
  # Apache Perú (hostname scaramutti-tms) apunta a :8085.
  # Rutea: /api/v1/* y /cotizaciones/* → v2 · todo lo demás → 302 a la SPA.
  # Desde el cutover ya NO sirve v1, así que como rollback devuelve v2, no el
  # sistema viejo.
  # ==========================================
  gateway:
    image: nginx:alpine
    container_name: scaramutti-tms-gateway-prod
    restart: always
    volumes:
      - ./gateway/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "8085:80"
    networks:
      - scaramutti-tms_default
    # OJO: nginx resuelve los nombres de TODOS los upstreams al arrancar, así
    # que los destinos declarados tienen que existir. Desde el cutover el
    # gateway ya no nombra ninguno de v1: arranca sin él.
    depends_on:
      frontend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://127.0.0.1/gateway/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 64M

networks:
  scaramutti-tms_default:
    external: true
