# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Env files: ningun .env* se commitea, EXCEPTO el template `.env.example`.
# Cada dev copia `.env.example` a `.env.local` (o `.env.<mode>.local`) y completa.
.env*
!.env.example
*.local

# Vite cache (regenerated on each dev run)
.vite/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
